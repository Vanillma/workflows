name: Github Workflows

on:
  workflow_dispatch:
    inputs:
      name:
        description: "What's your name?"
        required: true

jobs:
  helloWorld:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello ${{ github.event.inputs.name }}" > $GITHUB_STEP_SUMMARY
