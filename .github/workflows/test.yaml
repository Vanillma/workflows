name: Github Workflows

on: push

env:
  GLOG: "$GITHUB_STEP_SUMMARY"

jobs:
  helloWorld:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello Logger!" > $GLOG
